# Required metadata
sonar.projectKey=as-tsp.c
sonar.projectName=Simple Java project analyzed with the Sonar Runner
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=/home/haridian/Escritorio/local/pruebajenkins

# Comma-separated paths to directories with binaries (optional), in case of Java - directories with 

# Comma-separated paths to test source directories (optional)

# Comma-separated paths to files with third-party libraries (optional), in case of Java - JAR files


# Java is the default language, so the following property can be commented-out

